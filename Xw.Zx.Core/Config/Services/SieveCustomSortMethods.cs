using System.Linq;
using Sieve.Services;


namespace Xw.Zx.Core.Config
{
    public class SieveCustomSortMethods : ISieveCustomSortMethods
    {
        //public IQueryable<Products> HomeRecommendProduc(IQueryable<Products> source, bool useThenBy,bool des) => useThenBy
        //    ? ((IOrderedQueryable<Products>)source).ThenByDescending(p => p.ProCount)
        //    : source.OrderByDescending(p => p.ProCount);
    }
}
